# Optional backend API URL override for local development.
# Vercel production uses frontend /api rewrites, so this is not the primary production fix.
VITE_API_URL=http://localhost:3001

# Mapbox public access token — get one at https://account.mapbox.com
VITE_MAPBOX_TOKEN=your_mapbox_token_here
